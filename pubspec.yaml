name: movies_app
description: "A new Flutter project."

# يمنع نشر الحزمة على pub.dev
publish_to: 'none'

# رقم الإصدار
version: 1.0.0+1

# بيئة الـ SDK المطلوبة
environment:
  sdk: ^3.6.0

# 📌 الحزم المطلوبة في المشروع
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  introduction_screen: ^3.1.14
  shared_preferences: ^2.4.0
  carousel_slider: ^5.0.0
  video_player: ^2.9.2
  flutter_native_splash: ^2.4.4
  flutter_svg: ^2.0.17
  url_launcher: ^6.2.5
  flutter_bloc: ^8.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # 📌 إضافة الملفات والصور إلى التطبيق
  assets:
    - assets/
    - assets/images/
    - assets/icons/
